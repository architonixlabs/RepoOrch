{
  "name": "repo-orchestrator-triage-runner",
  "version": "0.1.0",
  "description": "Headless Agent SDK triage runner for repo-orchestrator",
  "type": "module",
  "scripts": {
    "lint": "eslint . --ext .mjs",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.2.111"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
